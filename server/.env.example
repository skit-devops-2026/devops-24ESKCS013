# CampusConnect — Environment Configuration
# Copy this file to .env and fill in your values. Never commit .env!

DB_HOST=db
DB_PORT=3306
DB_USER=campususer
DB_PASSWORD=campus123
DB_NAME=campusconnect

JWT_SECRET=campusconnect_super_secret_jwt_key_change_this
JWT_EXPIRES_IN=7d

# Leave as placeholder to auto-use Ethereal (test email).
# Replace with real Gmail credentials for production.
EMAIL_HOST=smtp.ethereal.email
EMAIL_PORT=587
EMAIL_USER=placeholder@ethereal.email
EMAIL_PASS=placeholder

PORT=5000
NODE_ENV=development
